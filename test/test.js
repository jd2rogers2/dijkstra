require('mocha');
var expect = require('chai').expect;
const { solution } = require('../solution');

describe('test', () => {
  it('test 1', () => {
    expect(solution()).to.equal(undefined);
  });
});
